# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# Python 侧车环境（AI:PRD-003 · 003-B）
# 🔴 环境不入 git，requirements.txt 入 git —— 装法见 sidecar/README.md
/sidecar/.venv-sidecar/
sidecar/.venv-sidecar/
__pycache__/
*.pyc

# database
# 🔴 本地 SQLite 库全部落在 data/，整目录不入 git（库文件 + WAL + 备份）
/data/
data/
*.db
*.db-journal
*.db-shm
*.db-wal
/prisma/db.sqlite
/prisma/db.sqlite-journal
db.sqlite

# 本地 ONNX 句向量模型（AI:PRD-004 · 004-A）
# 🔴 91MB 二进制，绝不进 git。换机/新 clone 跑一次重建：
#    powershell -File scripts/fetch-embed-model.ps1
# 🔴🔴 只留带锚点的 `/models/`（= 仓根那一个目录）。
#    曾经这里还有一行**没有锚点**的 `models/`，它会吞掉**任意层级**的同名目录 ——
#    2026-08-14 集成时实录：新建的 `src/app/api/models/` 在 `git status` 里根本不出现，
#    提交即静默丢文件（当时改名 `/api/exam-models` 绕开，见 src/app/api/exam-models/route.ts 文件头）。
#    往下加忽略规则时同理：目录名前面**必须**带 `/`，否则就是给未来埋同一颗雷。
/models/

# next.js
/.next/
# 🔴 生产构建产物（AI:PRD-009 验收修复）：与 dev 的 .next 分家。
#    共用一个目录时，跑着的 `next dev --turbo` 会覆盖 routes-manifest.json，
#    `next start` 当场 `TypeError: routesManifest.dataRoutes is not iterable`。
#    目录名同时写在 next.config.js（distDir）与 scripts/next-prod.mjs（DIST）里。
/.next-prod/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
# 🔴 凭据一律不入 git，只有 .env.example 可提交
# do not commit any .env files to git, except for the .env.example file. https://create.t3.gg/en/usage/env-variables#using-environment-variables
.env
.env.*
!.env.example
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# idea files
.idea