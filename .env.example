# 本文件是 .env 的模板，会进 git。🔴 里面绝不能放真凭据。
# 克隆仓库后：复制一份改名 .env，再按需填值。
# 新增环境变量时，三处同步：.env / .env.example / src/env.js 的 schema。

# Drizzle / libsql 本地库（路径相对项目根；data/ 整目录 gitignore，不会被提交）
DATABASE_URL="file:./data/资料库.db"

# 🔴🔴 圣域：批改线 审核.db 的【只读】挂载（G-1 红线）
#   - 本产品对它只有 SELECT / PRAGMA，物理零写路径；
#   - 连接串**必须**显式带 ?mode=ro —— core/grading.ts 校验不到就直接抛，不会替你补；
#   - 这不是凭据（就是本机一个文件路径），所以可以写真路径进模板。
GRADING_DB_URL="file:D:/workplace/ai-bkb/订阅特训/_产线/审核.db?mode=ro"

# 备份异地副本目录：每次 backupNow() 出快照后再复制一份过去。
#   - 不设 = 跳过异地（返回值里如实写 skipped，不静默）；
#   - 🔴 别把真实路径写进 git，本机的填在 .env 里（比如另一块盘 / 同步盘目录）。
# BACKUP_REMOTE_DIR="E:/backup/knowledge-factory"

# 当前向量模型版本（question_vec.embed_model_ver 的唯一合法值）。
#   对账 C3 用它判「混版」：库里有向量却没设这个值 = 红（等于没有版本闸）。
#   🔴 必须与 core/embed.ts 的 EMBED_MODEL_ID 一致 —— 对不上 embedTexts 当场抛
#      VERSION_MISMATCH（不一致最阴：向量会被标成一个它其实不是的版本，C3 反而绿）。
#   🔴 模型文件本身不在 git 里，新 clone 先跑：
#      powershell -File scripts/fetch-embed-model.ps1
EMBED_MODEL_VER="bge-small-zh-v1.5"
